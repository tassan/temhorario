# Só para `npm run db:migrate:test:docker` — mesma instância que `.env.docker`, base `temhorario_test` (Vitest).
MIGRATE_DATABASE_URL=postgresql://postgres:postgres@127.0.0.1:5433/temhorario_test?sslmode=disable
